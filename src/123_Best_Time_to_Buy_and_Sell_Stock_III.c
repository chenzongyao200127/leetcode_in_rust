// 123_Best_Time_to_Buy_and_Sell_Stock_III
// https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-iii/description/?envType=daily-question&envId=2023-10-09

#include <stdio.h>
#ifndef __cplusplus
#include <stdbool.h>
#endif

#define MAX(a, b) (((a) > (b)) ? (a) : (b))
#define MIN(a, b) (((a) < (b)) ? (a) : (b))

int maxProfit(int *prices, int pricesSize)
{
}